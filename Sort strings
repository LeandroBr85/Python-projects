1 - sorted strings or lists
### - sort based on element of a list.

k = int(input())
    arr.sort(key=lambda p: p[k])
    for x in arr:
        for s in x:
            print(s, end=" ")
        print()

### sorting strings
 lower = []
upper = []
oddi =[]
eddi = []

for l in a:
    if l.isupper() == True:
        upper.append(l)
    elif l.islower() == True:
        lower.append(l)
    elif int(l) % 2 == 0:
        oddi.append(l)
    else:
        eddi.append(l)

lower.sort()
upper.sort()
oddi.sort()
eddi.sort()

sort = lower + upper + eddi + oddi

sorted = ''
sorted = sorted.join(sort)
print(sorted)

count substrings.

def count_substring(string, sub_string):
    s = ([s for s in range(len(string)) if string.find(sub_string, s) == s])
    t = len(s)    
